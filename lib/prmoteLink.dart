import 'package:flutter/material.dart';
class promoteLink extends StatelessWidget {
  const promoteLink({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [
          Text("promote Link")
        ],
      ),
    );
  }
}
