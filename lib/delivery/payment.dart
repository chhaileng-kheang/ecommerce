import 'package:flutter/material.dart';
class paymentDelivery extends StatelessWidget {
  const paymentDelivery({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
